#include <stdio.h>

int main()
{
  int a[] = {1, 3, 9, 8, 4, 6, 7, 3};
  int N = sizeof(a) / sizeof(a[0]);
  int i, temp;

  temp = a[0]
  for (i = 0, i < N; i++){
    if(a[0] > a[i]){
      
    }
  }

  return 0;
}