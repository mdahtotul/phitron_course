#include <bits/stdc++.h>
using namespace std;

Struct {
  int val;
  TreeNode *left;
  TreeNode *right;
  TreeNode(int val) {
    this->val = val;
    left = NULL;
    right = NULL;
  }
};

class Solution {
public:
    TreeNode* sortedArrayToBST(vector<int>& nums) {
            
    }
};

int main() {
  ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  
  
  return 0;
}