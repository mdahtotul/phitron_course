#include <bits/stdc++.h>

using namespace std;

int main()
{
   string s1 = "I am eating rice. ";
   string s1 = "I am also eating fish.";
   string s3 = s1 + s2;

   cout<<s3<<endl;
   string s = "abcdef"
   s.pop_back(); //delete last character from a str
   s.erase(s.begin()); //delete first character from a str
   s.erase(s.begin()+1); //delete first index character from a str

   return 0;
}
